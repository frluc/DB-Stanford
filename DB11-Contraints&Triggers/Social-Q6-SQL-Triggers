CREATE TRIGGER Q6
AFTER UPDATE ON Likes
FOR EACH ROW
WHEN New.ID1 = Old.ID1
AND EXISTS (SELECT * FROM Friend WHERE ID1 = Old.ID2 AND ID2 = New.ID2) 
BEGIN
    DELETE FROM Friend
    WHERE ( ID1 = Old.ID2 AND ID2 = New.ID2 )
    OR ( ID1 = New.ID2 AND ID2 = Old.ID2 );
END;
